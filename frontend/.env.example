# Copy to .env and adjust. Never commit .env.
# Backend base URL used by the app (must be in Cors:AllowedOrigins server-side).
VITE_API_URL=http://localhost:5245
# Displayed next to the workshop name (empty = hidden).
VITE_PERIOD=
